<%= form_for(@d_nsize) do |f| %>
  <% if @d_nsize.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@d_nsize.errors.count, "error") %> prohibited this d_nsize from being saved:</h2>

      <ul>
      <% @d_nsize.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :size %><br />
    <%= f.text_field :size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
