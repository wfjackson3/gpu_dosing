<%= form_for(@dnsize) do |f| %>
  <% if @dnsize.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dnsize.errors.count, "error") %> prohibited this dnsize from being saved:</h2>

      <ul>
      <% @dnsize.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :size %><br />
    <%= f.text_field :size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
