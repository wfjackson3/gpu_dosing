<h1>Listing dn_sizes</h1>

<table>
  <tr>
    <th>Size</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @dn_sizes.each do |dn_size| %>
  <tr>
    <td><%= dn_size.size %></td>
    <td><%= link_to 'Show', dn_size %></td>
    <td><%= link_to 'Edit', edit_dn_size_path(dn_size) %></td>
    <td><%= link_to 'Destroy', dn_size, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dn size', new_dn_size_path %>
