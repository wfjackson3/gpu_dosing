<%= form_for(@valve) do |f| %>
  <% if @valve.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@valve.errors.count, "error") %> prohibited this valf from being saved:</h2>

      <ul>
      <% @valve.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :partnumber %><br />
    <%= f.text_field :partnumber %>
  </div>
  <div class="field">
    <%= f.label :head_id %><br />
    <%= f.collection_select :head_id, Head.all, :id, :material, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :gasket_id %><br />
    <%= f.collection_select :gasket_id, Gasket.all, :id, :material, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :ball_id %><br />
    <%= f.collection_select :ball_id, Ball.all, :id, :material, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :valvegroup_id %><br />
    <%= f.collection_select :valvegroup_id, Valvegroup.all, :id, :description, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :iskit %><br />
    <%= f.check_box :iskit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
