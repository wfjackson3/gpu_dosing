<h1>Listing headsizes</h1>

<table>
  <tr>
    <th>Size</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @headsizes.each do |headsize| %>
  <tr>
    <td><%= headsize.size %></td>
    <td><%= link_to 'Show', headsize %></td>
    <td><%= link_to 'Edit', edit_headsize_path(headsize) %></td>
    <td><%= link_to 'Destroy', headsize, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Headsize', new_headsize_path %>
