module PagesHelper
  # Fill this in so it returns the kit object if only 1 kit remains from the filter
  # Handle returning text explanations if the results are 0 or many kits
  # Should I add conditional statements to the find view to properly render the return of this helper?
end
